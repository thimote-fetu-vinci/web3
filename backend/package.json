{
  "name": "backend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "dev": "nodemon --exec npm run start",
    "//db:setup": "needed for generating prisma client with correct database url from .env file",
    "db:setup": "prisma generate --no-engine",
    "//prisma:start_db": "We need a specific version of prisma to fix a bug on windows",
    "prisma:start_db": "npx prisma dev @0.0.0-dev.202509301625"
  },
  "dependencies": {
    "@prisma/client": "^6.16.3",
    "cookie-parser": "~1.4.4",
    "cors": "^2.8.5",
    "debug": "~2.6.9",
    "express": "~4.16.1",
    "morgan": "~1.9.1"
  },
  "devDependencies": {
    "prisma": "^6.16.3"
  }
}
